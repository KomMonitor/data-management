version: '3.8'

networks:
  kommonitor:
    name: kommonitor
    driver: bridge

services:
  keycloak:
    image: kommonitor/keycloak:26.5.5-postgres
    entrypoint: /opt/keycloak/bin/kc.sh start-dev
    container_name: keycloak
    restart: unless-stopped
    volumes:
      - ./realms/realm-export.json:/opt/keycloak/data/import/realm.json
      - ./storage/keycloak-db/data:/opt/keycloak/data/h2
    command:
      - --import-realm
      - --features=admin-fine-grained-authz:v1,scripts,
    expose:
      - 8080
    ports:
      - 8080:8080
    networks:
      - kommonitor
    environment:
      - KC_BOOTSTRAP_ADMIN_USERNAME=admin
      - KC_BOOTSTRAP_ADMIN_PASSWORD=admin
      - KC_DB=dev-file
  kommonitor-db:
    image: postgis/postgis:15-master
    container_name: kommonitor-db
    restart: unless-stopped
    networks:
      - kommonitor
    ports:
      - 5432:5432
    volumes:
      - ./storage/kommonitor-db/data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=kommonitor
  kommonitor-proxy:
    image: nginx:latest
    container_name: kommonitor-proxy
    restart: unless-stopped
    ports:
      - 80:80
    volumes:
      - ./templates:/etc/nginx/templates
    environment:
      - NGINX_PORT=80
      - DATA_MANAGEMENT=host.docker.internal:8085
    networks:
      - kommonitor
  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin
    restart: unless-stopped
    ports:
      - 8083:80
    environment:
      - PGADMIN_DEFAULT_EMAIL=user@kommonitor.de
      - PGADMIN_DEFAULT_PASSWORD=kommonitor
    volumes:
      - ./storage/pgadmin:/var/lib/pgadmin
    networks:
      - kommonitor
