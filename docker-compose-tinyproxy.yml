services:
  # 1. small proxy to tunnel external requests. i.e. keycloak requests
  tinyproxy:
    image: monokal/tinyproxy:latest
    ports:
      - "6666:8888"
    environment:
      - BASIC_AUTH_USER=admin
      - BASIC_AUTH_PASSWORD=password
    command: ANY